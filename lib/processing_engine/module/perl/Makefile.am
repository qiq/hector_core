if WITH_SWIG
if WITH_PERL

perldir = $(libdir)/hector/modules/perl
perl_SCRIPTS = Module.pm TestInput.pm TestOutput.pm TestSimple.pm TestMulti.pm Dump.pm TestPerl.pm

EXTRA_DIST = ${perl_SCRIPTS}

endif
endif
