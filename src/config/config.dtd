<!-- robot config definition -->

<!ELEMENT config	(Main|ProcessingChain|Processor|Module|Queue)* >

<!-- common elements -->

<!ELEMENT param		EMPTY >
<!ATTLIST param
	name		CDATA		""
	value		CDATA		"" >

<!-- Main -->

<!ELEMENT Main		(processingChain|serverHost|serverPort|param)* >
<!ATTLIST Main
	id		ID		#REQUIRED >

<!ELEMENT processingChain	EMPTY >
<!ATTLIST processingChain
	ref		IDREF 		#REQUIRED >

<!ELEMENT serverHost	(#PCDATA) >
<!ELEMENT serverPort	(#PCDATA) >

<!-- ProcessingChain -->

<!ELEMENT ProcessingChain	(processor|queue)* >
<!ATTLIST ProcessingChain
	id		ID		#REQUIRED >

<!ELEMENT processor	EMPTY >
<!ATTLIST processor
	ref		IDREF 		#REQUIRED >

<!ELEMENT queue	EMPTY >
<!ATTLIST queue
	ref		IDREF 		#REQUIRED >

<!-- Processor -->

<!ELEMENT Processor	(type,threads,module+) >
<!ATTLIST Processor
	id		ID		#REQUIRED >

<!ELEMENT type		(#PCDATA) >
<!ELEMENT threads	(#PCDATA) >

<!ELEMENT module	EMPTY >
<!ATTLIST module
	ref		IDREF 		#REQUIRED >

<!-- Queue -->

<!ELEMENT Queue	(maxMemory,maxItems,srcProcessor,dstProcessor) >
<!ATTLIST Queue
	id		ID		#REQUIRED >

<!ELEMENT maxMemory	(#PCDATA) >
<!ELEMENT maxItems	(#PCDATA) >

<!ELEMENT srcProcessor	EMPTY >
<!ATTLIST srcProcessor
	ref		IDREF 		#REQUIRED
	filter		CDATA		"" >

<!ELEMENT dstProcessor	EMPTY >
<!ATTLIST dstProcessor
	ref		IDREF 		#REQUIRED
	priority	CDATA		"" >

<!-- Module -->

<!ELEMENT Module	(lib,param*) >
<!ATTLIST Module
	id		ID		#REQUIRED >

<!ELEMENT lib		EMPTY >
<!ATTLIST lib
	name		CDATA		"" >
